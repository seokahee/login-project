// const { data } = useQuery({
//   queryKey: ["todos"],
//   queryFn: () => getTodos(),
// });

// console.log("data",data);

const TodoList = () => {
  return <div>TodoList</div>;
};

export default TodoList;
